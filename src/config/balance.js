// ============================================================
//  БАЛАНС  —  все числа игры в одном месте
// ============================================================

export const BALANCE = {
  // --- награда ---
  TASK_REWARD: 25,
  TASK_REWARD_CLEAN: 35,
  CLEAN_JUMP_LIMIT: 5,
  TASKS_PER_DAY: 3,

  // --- физика прыжка ---
  // MAX_DRAG маленький нарочно: полная сила набирается коротким движением,
  // не надо тянуть палец через весь экран.
  GRAVITY: 1300,
  LAUNCH_POWER: 9.5,
  MAX_DRAG: 130,
  MIN_DRAG: 14,

  // Кот — не пушка. Дуга показывает намерение, а прыгает он «примерно так».
  // 0.13 = ±13 % к силе, плюс небольшой увод по углу.
  JUMP_SPREAD: 0.13,
  JUMP_SPREAD_ANGLE: 0.06,   // радианы, это примерно ±3.5°

  // --- отскок от стен и потолка ---
  BOUNCE_X: 0.30,
  BOUNCE_Y: 0.15,
  CEILING_Y: 100,            // потолок ровно под шапкой интерфейса

  // --- приземление ---
  SLIDE_FACTOR: 0.05,
  SLIDE_MS: 260,
  WALK_SPEED: 200,
  // Сколько миллисекунд после прыжка кот не замечает площадку,
  // с которой прыгнул. Без этого он «толкается» в собственный стол
  // вместо того, чтобы спрыгнуть с него вниз.
  DROP_THROUGH_MS: 380,

  // --- прицел ---
  AIM_DOTS: 12,              // было 18; 12 — это те самые 65 % длины
  AIM_STEP: 0.05,
  AIM_COLOR: 0xFFFFFF,       // белая дуга

  // --- люстра ---
  HANG_RADIUS: 48,           // насколько близко надо пролететь, чтобы зацепиться
  HANG_COOLDOWN_MS: 450,     // чтобы не прицепиться обратно сразу после прыжка

  // --- хозяин ---
  OWNER_DELAY_MIN: 300,
  OWNER_DELAY_MAX: 900,
  OWNER_ACTIVE_MS: 320,
  WAKES_TO_FEED: 5,          // на пятой побудке хозяин сдаётся и кормит кота
  FEED_FISH: 4,

  // --- падающие предметы ---
  FRAGILE_CHANCE: 0.5,      // шанс, что лампа или телевизор упадут
  PILLOW_SPEED: 330,        // скорость подушки: медленно, чтобы успеть увернуться
  PILLOW_HIT_RADIUS: 34,    // насколько близко должна пролететь, чтобы сбить
  PILLOW_WARN_MS: 420       // сколько хозяин замахивается, прежде чем бросить
};
