# Библиотеки — не хранятся в репозитории.
# Восстанавливаются командой npm install из package.json
node_modules/

# Собранная версия — пересобирается командой npm run build
dist/
.vite/

# Секреты: токен бота, ключи платежей. НИКОГДА не попадают в репозиторий
.env
.env.local

# Мусор системы и редактора
.DS_Store
Thumbs.db
*.log
